# Path to your Firebase service-account key, absolute, or relative to this
# folder. The default expects service-account.json right here (gitignored).
# NEVER commit the key itself.
SERVICE_ACCOUNT_PATH=./service-account.json
