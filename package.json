{
  "name": "jobpilot-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "JobPilot, a self-hosted crew of Claude agents that runs your job search, with a React dashboard on your own Firebase.",
  "license": "MIT",
  "workspaces": [
    "app",
    "landing",
    "mcp-server"
  ],
  "scripts": {
    "dev:app": "npm run dev --workspace app",
    "dev:landing": "npm run dev --workspace landing",
    "build": "npm run build --workspace app && npm run build --workspace landing",
    "build:app": "npm run build --workspace app",
    "build:landing": "npm run build --workspace landing",
    "audit:secrets": "node scripts/audit-personal-data.mjs"
  }
}
